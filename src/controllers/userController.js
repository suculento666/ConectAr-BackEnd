// userController.js - maneja todo lo relacionado al usuario

// registerUser - crea un usuario nuevo
const registerUser = (req, res) => {
  res.json({ message: 'registerUser - próximamente' });
};

// loginUser - loguea un usuario y devuelve token
const loginUser = (req, res) => {
  res.json({ message: 'loginUser - próximamente' });
};

// getUserById - trae info del usuario + intereses + reputación
const getUserById = (req, res) => {
  res.json({ message: 'getUserById - próximamente' });
};

// updateUser - edita perfil
const updateUser = (req, res) => {
  res.json({ message: 'updateUser - próximamente' });
};

export { registerUser, loginUser, getUserById, updateUser };
